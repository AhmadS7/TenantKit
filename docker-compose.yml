version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: tenantkit-postgres
    ports:
      - '5432:5432'
    environment:
      POSTGRES_DB: tenantkit
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d tenantkit"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: tenantkit-redis
    ports:
      - '6379:6379'
    volumes:
      - redisdata:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tenantkit-backend
    ports:
      - '3000:3000'
    environment:
      NODE_ENV: production
      PORT: 3000
      DB_HOST: postgres
      DB_PORT: 5432
      DB_USERNAME: postgres
      DB_PASSWORD: postgres
      DB_DATABASE: tenantkit
      REDIS_HOST: redis
      REDIS_PORT: 6379
      JWT_SECRET: default_production_jwt_secret_change_me
      STRIPE_API_KEY: change_me
      STRIPE_WEBHOOK_SECRET: change_me
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: tenantkit-frontend
    ports:
      - '3001:3000'
    environment:
      NODE_ENV: production
      PORT: 3000
      NEXT_PUBLIC_API_URL: http://localhost:3000/v1
    depends_on:
      - backend

volumes:
  pgdata:
  redisdata:
